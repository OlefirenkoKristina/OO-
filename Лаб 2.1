using System;

class Program
{
    static void Main()
    {
        int rows = 5;
        int cols = 5;
        int[,] data = new int[rows, cols];
        
        Random rand = new Random();
        for (int i = 0; i < rows; i++)
        {
            for (int j = 0; j < cols; j++)
            {
                data[i, j] = rand.Next(0, 100);
            }
        }
        
        for (int i = 0; i < rows; i++)
        {
            for (int j = 0; j < cols; j++)
            {
                Console.Write(data[i, j] + "\t");
            }
            Console.WriteLine();
        }
    }
}
